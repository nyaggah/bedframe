import { describe, it } from 'vitest'

describe('service worker', () => {
  it('should define onInstalled listener', () => {
    console.log('test service-worker')
  })
  it('should define onClicked listener', () => {
    console.log('test service-worker')
  })
})

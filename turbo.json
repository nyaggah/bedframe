{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "globalEnv": ["TS_NODE_PROJECT", "NODE_ENV"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "dev": {
      "cache": false
    },
    "build:link": {
      "dependsOn": ["build"]
    },
    "lint": {
      "outputs": []
    },
    "format": {},
    "release": {
      "dependsOn": ["build"]
    }
  }
}
